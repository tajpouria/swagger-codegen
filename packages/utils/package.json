{
  "name": "@swagger-codegen/utils",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "tsc:watch": "tsc -w",
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^3.8.2"
  }
}
